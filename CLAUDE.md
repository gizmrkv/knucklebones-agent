# CLAUDE.md

## コーディングルール

### 環境・ツール

- Python 3.11 を使う。
- 環境・依存は **uv** で管理する（`pip` を使わない）。
- CLI引数のパースには **tyro** を使う。`argparse` / `typer` / `hydra` は使わない。
- 学習には **PyTorch** を使う。
- pre-commit で `ruff format` / `ty check` を実行し、パスしなかったら修正する。

### コーディングスタイル

- 関数・メソッドの引数および返り値には必ず型注釈を書く。
- パスは `pathlib` を使う。
- データフレーム処理には `polars` を使う。`pandas` は基本禁止。ただし `pandas` にしか対応していない外部ライブラリを使う場合は、直前に `to_pandas()` して良い。
- 時間がかかる処理は進捗を可視化する（`tqdm` など使用可）。
- `marimo` では UI コントロール（ドロップダウン・スライダー等）とその結果表示は、必ず同じ出力ブロックにまとめて表示する。
- 新しい config 項目を追加する際は必ずデフォルト値を持たせる（既存 config との後方互換性を保つ）。

### ログ・エラーハンドリング

- 例外を握りつぶすことを禁止する。失敗は必ず表面化させる（ログ出力・再送出のいずれか）。
- 標準出力への `print` を禁止し、`structlog` を使う。
- ログは必ずファイルに出力してトレースできるようにする。
- ログには常にタイムスタンプ・ステータス・メッセージを含める。必要に応じて追加する。
- **例外メッセージ（`raise ValueError("...")` 等）・ログの文言（`log.info("...")` 等）は英語で書く。** コードコメント・docstring・`README.md` 等のドキュメントは日本語のままでよい。

### Git 運用

- ローカルコミットはユーザーの確認を取らずに自律的に行ってよい。
- コミットメッセージは [Conventional Commits](https://www.conventionalcommits.org/) に従う: `<type>(<scope>): <description>`
  - `type`: `feat` / `fix` / `docs` / `refactor` / `test` / `perf` / `chore` から変更内容に最も合うものを選ぶ
  - `description`: 変更内容を1行で（日本語で可）
- 1コミット1論理単位でまとめる（無関係な変更を混ぜない）。

---

## 保護ファイルの編集権限

以下は **ユーザーの明示的な許可がない限り編集禁止**。

- `.claude/settings.json`（権限設定の変更はユーザー自身が行うため）
- `CLAUDE.md` 自体

---

## 失敗の記録（`.claude/failures.md`）

- 記録対象は **Claude Code 自身のミス**（実装・ツール呼び出し・パス関連など）であり、実験結果に関する失敗（精度・勝率が改善しないなど）は含めない。
  - 自分で気づいて自己解決したミスも対象に含む。外部（レビューAgent・ユーザー）から指摘されたかどうかは問わない。
  - ユーザーからの修正指示・変更指示も対象に含む（単なる次工程への指示は対象外）。
- **既存のエントリと同種・類似のミスであっても、重複扱いで省略せず必ず新規エントリとして追記する。** 統合・上書きはしない。
- 「日時・症状・原因・対処」の4項目を各50字程度で追記する。後で同じエラーに遭遇したとき grep で見つけられることを目的とする。
- 追記はフェーズ移行のような状態変更を伴わない、低リスクな操作のため、Claude が自律的に行ってよい（都度確認不要）。
- 記録はミスに気づいた直後に行う。

```markdown
# Failures

日時: YYYY-MM-DD
症状: 失敗の概要。50字程度
原因: 考えられる失敗の原因。50字程度
対処: その時取った対処。50字程度
```
